{
    "name": "cppsarif",
    "displayName": "cppsarif",
    "description": "cppsarif",
    "icon": "share/package.png",
    "publisher": "shyeyian",
    "version": "1.1.47",
    "engines": {
        "vscode": "^1.100.0"
    },
    "categories": [
        "Language Packs"
    ],
    "repository": {
        "type": "git",
        "url": "https://github.com/shyeyian/vscode-cppsarif"
    },
    "main": "main/cppsarif.js",
    "activationEvents": [
        "onStartupFinished"
    ],
    "contributes": {
        "views": {
            "sarif": [
                {
                    "id": "sarifView",
                    "name": "%title%",
                    "icon": "$(issues)"
                }
            ]
        },
        "viewsContainers": {
            "activitybar": [
                {
                    "id": "sarif",
                    "title": "%title%",
                    "icon": "$(issues)"
                }
            ]
        },
        "configuration": {
            "title": "cppsarif",
            "properties": {
                "cppsarif.sarifDirectory": {
                    "type": "string",
                    "markdownDescription": "Path to compiler output sarif directory. Requires: containing single or multiple `.sarif` files. Default: `.`",
                    "default": "."
                }
            }
        }
    }
}
